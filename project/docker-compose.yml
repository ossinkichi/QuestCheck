version: "3.8"
services:
    service-pg:
        image: bitnami/postgresql
        ports:
            - "5432:5432"
        environment:
            - POSTGRES_USER=docker
            - POSTGRES_PASSWORD=docker
            - POSTGRES_DB=quests_check
networks:
    app-network:
        driver: bridge
